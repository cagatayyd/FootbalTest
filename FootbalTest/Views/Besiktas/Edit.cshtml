
@{
    ViewBag.Title = "Edit";
}



@using (Html.BeginForm("Edit", "Besiktas", FormMethod.Post))
{
    <div class="container">
        <div class="row">
            <div class="col-9"></div>
            <div class="form-group col-3 text-right mt-3 mb-3 col-3">
                <h2>Edit</h2>
                <input type="submit" value="Update" class="btn btn-primary" />
                <input type="submit" value="Delete" class="btn btn-danger" formaction="@Url.Action("DeleteBesiktas", "Besiktas", new { id = Model.PlayerID })" />
                <a href="@Url.Action("GetPlayersBesiktas", "Besiktas")" class="btn btn-default">Cancel</a>
            </div>
        </div>
    </div>

            @Html.Partial("_CreateOrUpdate");
}


